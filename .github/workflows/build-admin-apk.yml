name: Build Electrofelec Admin APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: yes | sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Prepare Gestor completo mobile UI v2.3
        run: |
          mkdir -p android-admin/src/main/assets
          cat android-admin/ui/v13gz_*.part > /tmp/admin_v13.gz.b64
          base64 -d /tmp/admin_v13.gz.b64 | gzip -dc > android-admin/src/main/assets/index.html
          python android-admin/patch_v14.py
          python android-admin/patch_v20.py
          python android-admin/patch_v21.py
          python android-admin/patch_v22.py
          python android-admin/patch_v23.py
          test -s android-admin/src/main/assets/index.html
          grep -q "Electrofelec Admin v2.3" android-admin/src/main/assets/index.html
          grep -q "APP v2.3" android-admin/src/main/assets/index.html
          grep -q 'id="secMyPunch"' android-admin/src/main/assets/index.html
          grep -q 'data-mobile-tab="MyPunch"' android-admin/src/main/assets/index.html
          grep -q 'Nuestro fichaje' android-admin/src/main/assets/index.html
          grep -q 'INICIAR JORNADA · 8 H' android-admin/src/main/assets/index.html
          grep -q 'FIX definitivo Fichajes + Registro horas 21→20' android-admin/src/main/assets/index.html
          grep -q 'g22Period' android-admin/src/main/assets/index.html
          grep -q '/admin/start-admin-day' android-admin/src/main/assets/index.html
          grep -q '/admin/time-range' android-admin/src/main/assets/index.html
          grep -q 'electrofelec-gestor-sync' android-admin/src/main/assets/index.html

      - name: Validate Admin JavaScript
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          html=Path('android-admin/src/main/assets/index.html').read_text(encoding='utf-8')
          scripts=re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>',html,re.S|re.I)
          if not scripts:
              raise SystemExit('No se encontró JavaScript inline')
          Path('/tmp/electrofelec_admin.js').write_text('\n'.join(scripts),encoding='utf-8')
          print('JS extraído:',sum(len(x) for x in scripts),'bytes')
          PY
          node --check /tmp/electrofelec_admin.js

      - name: Build debug APK
        run: gradle :android-admin:assembleDebug

      - name: Verify APK
        run: |
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose android-admin/build/outputs/apk/debug/android-admin-debug.apk
          "$ANDROID_HOME/build-tools/35.0.0/aapt" dump badging android-admin/build/outputs/apk/debug/android-admin-debug.apk | tee /tmp/badging.txt
          grep -q "versionCode='9'" /tmp/badging.txt
          grep -q "versionName='2.3-admin'" /tmp/badging.txt

      - name: Prepare artifact
        run: |
          cp android-admin/build/outputs/apk/debug/android-admin-debug.apk Electrofelec_Admin_v2_3_GESTOR_COMPLETO.apk
          test -s Electrofelec_Admin_v2_3_GESTOR_COMPLETO.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Electrofelec_Admin_v2_3_GESTOR_COMPLETO
          path: Electrofelec_Admin_v2_3_GESTOR_COMPLETO.apk
          if-no-files-found: error
